name: natgard_shop
description: "Натуральные домашние продукты."
homepage: https://natgard.ru/
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  melos: ^6.1.0
  admin_web:
    path: packages/admin_web
  client_web:
    path: packages/client_web
  client_mobile:
    path: packages/client_mobile
  shared:
    path: packages/shared
  audioplayers: ^6.1.0
  flutter_file_uploader: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true


